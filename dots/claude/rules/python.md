# Python

- Use `uv run` to run all Python tools: `uv run python`, `uv run pytest`, `uv run ruff`, etc.
- Never invoke `python`, `pytest`, `ruff`, etc. directly — they may not resolve to the project's virtualenv.
