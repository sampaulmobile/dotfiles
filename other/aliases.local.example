# Machine-local aliases & functions — sourced by ~/.zshrc after the shared
# aliases, so anything here can override them. Private or machine-specific
# commands, host shortcuts, and env exports live here, never in git.
# To use: cp aliases.local.example aliases.local && edit.

# alias myproj="cd $HOME/dev/myproject"
